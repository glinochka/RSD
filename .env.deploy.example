# Copy to .env.deploy on the VPS (not committed).
REGISTRY=ghcr.io/YOUR_GITHUB_USERNAME
IMAGE_TAG=main

# Optional: PAT with read:packages for private GHCR images
# GHCR_USER=deploy
# GHCR_TOKEN=ghp_...
